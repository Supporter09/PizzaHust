# Admin A5-A10 Completion Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Complete the unfinished admin workstreams by finishing the contract-backed A5-A7 pages first, then extending the admin catalog/combo experience for A8-A10 using the `Design/` mockups as the visual source of truth.

**Architecture:** Keep the backend as the source of truth for business rules and validation, then let the Next.js admin pages render from those APIs with the existing design tokens/components. Treat `Design/` as read-only reference material only; do not modify any files under that folder. Split the work into two tranches: A5-A7 (current contract) and A8-A10 (planned deltas from `CONTRACTS.md` and the design mockups).

**Tech Stack:** FastAPI, SQLAlchemy, pytest, Next.js App Router, TypeScript, Playwright, OpenAPI type generation.

---

## Source Map

| Use case | Backend contract | Frontend route(s) | Design source |
|---|---|---|---|
| A5 Monitor Orders | `backend/app/api/admin/orders.py` | `frontend/app/admin/orders/page.tsx` | `Design/admin-orders.html` |
| A6 Customer Accounts | `backend/app/api/admin/customers.py` | `frontend/app/admin/customers/page.tsx`, `frontend/app/admin/customers/[id]/page.tsx` | `Design/admin-customers.html`, `Design/admin-customer-detail.html` |
| A7 Sales Reports | `backend/app/api/admin/reports.py` (new) | `frontend/app/admin/reports/page.tsx` (new) | `Design/admin-reports.html` |
| A8 Generic Options Model | `backend/app/api/admin/options.py` replacement path (new contract) | `frontend/app/admin/pizza-options/page.tsx` and item editor surfaces | `Design/admin-item-edit.html`, `Design/screens/item-edit-options.png`, `Design/screens/item-edit-opts.png` |
| A9 Multi-image Dishes | `backend/app/api/admin/items.py` image workflow expansion | `frontend/app/admin/items/page.tsx` and item edit flow | `Design/admin-item-edit.html`, `Design/screens/gallery-check.png` |
| A10 Combo Choice-Slots and Picker | `backend/app/api/admin/combos.py` expansion | `frontend/app/admin/combos/page.tsx` and combo editor flow | `Design/admin-combos.html`, `Design/admin-combo-edit.html`, `Design/screens/combo-picker.png` |

## Current Gaps

- `/admin/reports` does not exist in the frontend yet.
- `backend/app/api/admin/reports.py` does not exist yet.
- `CONTRACTS.md` already defines `GET /api/admin/reports/sales`, but `frontend/lib/api/types.ts` does not expose a report schema yet.
- The current `admin/page.tsx` redirects to `/admin/orders`; the design includes a dashboard, but that is not required for the A5-A10 workstream unless we explicitly decide to add an admin landing page later.
- `Design/` already contains the reference HTML/screens; do not change anything there.

## Tranche 1: A5-A7 Contract-Backed Admin Completion

### Tranche 1 Scope Lock

- A5 ships the existing order monitoring table flow: status filters, dispatch-pending warning, retry dispatch, and cancel support if surfaced by the UI. Do not add an order detail route unless a later requirement explicitly asks for it.
- A6 ships the customer list, search, detail page, and lock/unlock controls. The backend remains the source for loyalty points, tier, order count, and locked state.
- A7 is the only net-new backend/frontend surface in this tranche: add `/api/admin/reports/sales`, regenerate OpenAPI types, add `/admin/reports`, and link it from the admin nav.
- Do not modify files under `Design/`. Use them only as visual references.

### Tranche 1 Implementation Status

Implemented on 2026-06-10:
- A5 backend coverage was tightened for status filtering, pagination, retry dispatch, and cancel behavior.
- A6 backend coverage was added for search, detail, missing customer, lock, and unlock.
- A7 backend route `/api/admin/reports/sales` was added with daily/weekly grouping plus JSON and CSV output.
- A7 frontend route `/admin/reports` was added and linked from the admin sidebar.
- `Application/openapi.json` and `Application/frontend/lib/api/types.ts` were regenerated.

Verification completed:
- `Application/backend`: `ruff check app tests`, `ruff format --check app tests`, `mypy app/domain`.
- `Application/backend`: focused A5-A7 tests, `16 passed`.
- `Application/backend`: full pytest outside sandbox with required seed env, `195 passed, 1 skipped`.
- `Application/frontend`: `npx tsc --noEmit`, `npm run lint`, `npx vitest run --exclude 'tests/e2e/**' --passWithNoTests`, `npm run build`.

### Task 1: Harden A5 Order Monitoring Backend and API Shape

**Files:**
- Modify: `Application/backend/app/api/admin/orders.py`
- Modify: `Application/backend/tests/test_admin_orders_dispatch.py`

- [x] **Step 1: Write/extend failing tests**

Add coverage for:
- status filter validation (`400` on invalid enum values)
- retry dispatch success path returning `204` and persisting `Delivering`
- retry dispatch conflict path when the order is not `DispatchPending`
- list endpoint pagination behavior if the current frontend needs `page`/`page_size`
- order cancel success path for cancellable statuses

- [x] **Step 2: Run the red test set**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_orders_dispatch.py -q`

Expected: one or more new assertions fail before the route behavior is aligned with the intended UI flow.

- [x] **Step 3: Implement minimal backend adjustments**

Keep the route contract aligned with `CONTRACTS.md`. If the UI needs extra fields for the design, add them explicitly in the response model rather than guessing them in the frontend.

- [x] **Step 4: Run the green test set**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_orders_dispatch.py -q`

Expected: pass.

### Task 2: Finish A5 Orders Frontend to Match `Design/admin-orders.html`

**Files:**
- Modify: `Application/frontend/app/admin/orders/page.tsx`
- Modify: `Application/frontend/components/shared/status-badge.tsx` if the design requires additional status variants
- Add: `Application/frontend/tests/e2e/admin-orders.spec.ts`

- [x] **Step 1: Write the failing UI test**

Assert the orders page:
- renders the order table
- shows the dispatch warning banner when `DispatchPending` exists
- supports filtering by status chip
- exposes the retry action only for `DispatchPending`

- [ ] **Step 2: Run the red UI test**

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-orders.spec.ts`

Expected: fail until the page matches the design and API shape.

Note: not run in this session because Playwright requires a running frontend/backend stack. The spec was added and frontend build/type/lint gates passed.

- [x] **Step 3: Implement the UI**

Match the table/filter/badge layout from `Design/admin-orders.html`.
Prefer the existing admin layout shell and design tokens.

- [ ] **Step 4: Run the green UI test**

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-orders.spec.ts`

Expected: pass.

Note: pending a running stack for browser e2e.

### Task 3: Finish A6 Customer Accounts Backend and Frontend

**Files:**
- Modify: `Application/backend/app/api/admin/customers.py`
- Create: `Application/backend/tests/test_admin_customers.py`
- Modify: `Application/backend/tests/admin_test_utils.py` if shared customer fixtures are needed
- Modify: `Application/frontend/app/admin/customers/page.tsx`
- Modify: `Application/frontend/app/admin/customers/[id]/page.tsx`
- Add: `Application/frontend/tests/e2e/admin-customers.spec.ts`

- [x] **Step 1: Write failing tests for the detail flow**

Add backend assertions for:
- detail lookup returning `404` when the customer does not exist
- lock/unlock preserving the customer account state
- search by name/phone/email matching the contract

Add frontend assertions for:
- customer list rendering
- search filtering
- opening the detail page
- lock/unlock action feedback

- [x] **Step 2: Run the red test set**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_customers.py -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-customers.spec.ts`

Expected: the new assertions fail until the list/detail flows are aligned.

- [x] **Step 3: Implement the backend/front updates**

Keep the backend as the canonical data source for tier, points, order count, and lock state.
Keep the detail page visually aligned with `Design/admin-customer-detail.html`.

- [x] **Step 4: Run the green test set**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_customers.py -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-customers.spec.ts`

Expected: pass.

### Task 4: Add A7 Sales Reports End-to-End

**Files:**
- Create: `Application/backend/app/api/admin/reports.py`
- Modify: `Application/backend/app/main.py` or the router wiring if needed
- Create: `Application/backend/tests/test_admin_reports.py`
- Modify: `Application/backend/app/infra/db/models.py` only if the report query needs a new aggregate helper
- Modify: `Application/frontend/lib/api/types.ts` after regenerating types
- Create: `Application/frontend/app/admin/reports/page.tsx`
- Add: `Application/frontend/tests/e2e/admin-reports.spec.ts`

- [x] **Step 1: Write failing backend and frontend tests**

Backend tests should prove:
- `GET /api/admin/reports/sales?from=...&to=...` returns daily or weekly aggregates
- the response includes `date`, `order_count`, `revenue_vnd`, and `top_items`
- `?format=csv` returns CSV output

Frontend tests should prove:
- the reports page renders the date range controls
- the summary table renders the expected fields
- CSV export is discoverable from the UI

- [x] **Step 2: Run the red test set**

Run: `cd Application/backend && .venv/bin/pytest -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-reports.spec.ts`

Expected: the report-specific assertions fail before the endpoint and page exist.

- [x] **Step 3: Implement the report endpoint and page**

Use the design page as the visual reference and keep the API response shape aligned with `CONTRACTS.md`.
If the frontend needs extra formatting helpers, keep them in `frontend/lib/format.ts` or a nearby report helper, not in the page component itself.

- [x] **Step 4: Regenerate types and rerun tests**

Run: `cd Application/frontend && npm run gen:types`

Run: `cd Application/backend && .venv/bin/pytest -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-reports.spec.ts`

Expected: pass.

## Tranche 2: A8-A10 Design-Driven Admin Expansion

### Task 5: Replace Fixed Pizza Options With Generic Option Groups

**Files:**
- Create or expand: `Application/backend/app/api/admin/option_groups.py` or the chosen replacement module
- Modify: `Application/backend/app/api/admin/options.py` if it remains as a compatibility layer
- Modify: `Application/backend/app/infra/db/models.py`
- Modify: `Application/backend/tests/test_admin_options.py`
- Modify: `Application/frontend/app/admin/pizza-options/page.tsx`
- Modify: `Application/frontend/app/admin/items/page.tsx` or create an item editor page if the design requires it
- Create: `Application/frontend/components/admin/option-group-editor.tsx` if the editor is split out
- Add or update: `Application/frontend/tests/e2e/admin-options.spec.ts`

- [ ] **Step 1: Write failing tests for the new contract**

Tests should cover:
- creating, editing, disabling, and deleting option groups
- nested option CRUD with `name`, `description`, `price_delta_vnd`, `enabled`
- existing item/customizer flows resolving option groups instead of fixed size/crust/topping tables

- [ ] **Step 2: Run the red tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_options.py -q`

Expected: fail once the generic model replaces the fixed tables.

- [ ] **Step 3: Implement the new option-group model**

Keep backwards compatibility only if the rest of the app still depends on the old tables.
Update the admin UI to match `Design/admin-item-edit.html` and the screenshots under `Design/screens/`.

- [ ] **Step 4: Rerun the tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_options.py -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-options.spec.ts`

Expected: pass.

### Task 6: Add Multi-Image Dish Management

**Files:**
- Modify: `Application/backend/app/api/admin/items.py`
- Modify: `Application/backend/app/infra/db/models.py` and migrations if image storage needs schema changes
- Modify: `Application/backend/tests/test_admin_items_image.py`
- Modify: `Application/frontend/app/admin/items/page.tsx`
- Create: `Application/frontend/components/admin/item-gallery.tsx` if the gallery is split out
- Add: `Application/frontend/tests/e2e/admin-item-images.spec.ts`

- [ ] **Step 1: Write failing tests**

Backend tests should cover:
- uploading multiple images
- marking one image as the cover
- deleting an image and auto-falling back to another cover when needed

Frontend tests should cover:
- the gallery section rendering in the item editor
- cover switching and delete actions

- [ ] **Step 2: Run the red tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_items_image.py -q`

Expected: fail on the new gallery behavior before implementation.

- [ ] **Step 3: Implement the gallery workflow**

Use `Design/admin-item-edit.html` and `Design/screens/gallery-check.png` as the visual reference.

- [ ] **Step 4: Run the green tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_items_image.py -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-item-images.spec.ts`

Expected: pass.

### Task 7: Add Combo Choice-Slots and the Combo Picker Flow

**Files:**
- Modify: `Application/backend/app/api/admin/combos.py`
- Modify: `Application/backend/app/infra/db/models.py`
- Modify: `Application/backend/tests/test_admin_combos.py`
- Modify: `Application/frontend/app/admin/combos/page.tsx`
- Create or modify: `Application/frontend/app/admin/combos/[id]/page.tsx` if the design needs a dedicated editor route
- Create: `Application/frontend/components/admin/combo-component-picker.tsx` if the picker is split out
- Add: `Application/frontend/tests/e2e/admin-combos.spec.ts`

- [ ] **Step 1: Write failing tests for choice slots**

Tests should cover:
- editable combo components with either fixed products or customer-choice slots
- category-scoped choice-slot validation
- combo picker UI actions mirroring the design
- over-price warning behavior still being allowed

- [ ] **Step 2: Run the red tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_combos.py -q`

Expected: fail before the choice-slot model exists.

- [ ] **Step 3: Implement the combo model expansion**

Use `Design/admin-combos.html`, `Design/admin-combo-edit.html`, and `Design/screens/combo-picker.png` as the visual source.

- [ ] **Step 4: Run the green tests**

Run: `cd Application/backend && .venv/bin/pytest tests/test_admin_combos.py -q`

Run: `cd Application/frontend && npx playwright test tests/e2e/admin-combos.spec.ts`

Expected: pass.

## Final Verification

### Task 8: Repo-Wide Acceptance

**Files:**
- Modify: `Application/feature_list.json`
- Modify: `Application/progress.md`
- Modify: `Application/session-handoff.md`

- [ ] **Step 1: Run backend verification**

Run: `cd Application/backend && .venv/bin/pytest -q`

Expected: all admin and existing regression tests pass.

- [ ] **Step 2: Run frontend verification**

Run: `cd Application/frontend && npm run lint && npm run build && npx playwright test`

Expected: lint/build/e2e pass for the admin flows.

- [ ] **Step 3: Run the repo gate**

Run: `cd Application && ./verify.sh`

Expected: exit 0.

- [ ] **Step 4: Update tracking docs**

Record the commit hash and timestamp in `Application/feature_list.json`, then update `Application/progress.md` and `Application/session-handoff.md` with the next remaining feature and the blockers that were resolved.
